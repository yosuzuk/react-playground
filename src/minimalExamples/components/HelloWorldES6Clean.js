import React from 'react';

/**
 * Probably the cleanest version of Hello World!
 *
 * - uses functional component syntax
 * - is named to help debugging
 * - uses separate lines for JSX and JavaScript
 * - wraps JSX in parenthesis
 * - uses semicolons
 */
const HelloWorld = () => (
    <div>Hello World!</div>
);

export default HelloWorld;
